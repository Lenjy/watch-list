<h1> Add a new movie to your list</h1>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: Movie.all, prompt: "Choose a Movie" %>
  <%= f.button :submit %>
<% end %>

<%= link_to "return", list_path(@list) %>